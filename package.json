{
  "scripts": {
    "docker:build": "docker build -t php-server docker/ && docker compose build --no-cache",
    "docker:dev": "docker build -t php-server docker/ && docker compose build && docker compose up -d",
    "docker:production": "docker build -t php-server docker/ && docker compose build && docker compose --env-file docker/.env.production up -d",
    "docker:down": "docker compose down --volumes --remove-orphans"
  }
}
